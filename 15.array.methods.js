// reverse
// => reverses an array
// const numList = [55, 65, 11, 23];

// numList.reverse();

// console.log(numList);

// sort

// const nameList = ['Sumita', 'Raush', 'Aayush', 'Bibisha', 'Sajan', 'Ram'];
// ascending sort
// nameList.sort();

// descending string
// nameList.sort().reverse();
// console.log(nameList);

// const numList = [20, 1, 255, 10, 110, 30, 3];
// numList.sort((a, b) => {
//   return b - a; //descending sort
// });

// numList.sort((a, b) => {
//   return a - b; //ascending sort
// });

// console.log(numList);

// ? split
const fullName = 'Lomus Maharjan';
const splittedValue = fullName.split(' ');
// console.log(splittedValue);
const firstName = splittedValue[0];
const lastName = splittedValue[1];
console.log(firstName);
